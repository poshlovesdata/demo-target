# Demo Calculator

A tiny disposable Python repository for a coding task.

Run the tests with:

```sh
python -m pytest
```

The intentionally broken feature is `calculator.divide`. Implement it so that
normal division succeeds and division by zero raises `ZeroDivisionError`.
